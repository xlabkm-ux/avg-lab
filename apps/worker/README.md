# apps/worker

Async workers: document ingestion, graph sync, eval runs, reports.

## First Implementation Tasks

- Add package.json.
- Add TypeScript config extending root.
- Add health/smoke test.
- Add README usage examples.
